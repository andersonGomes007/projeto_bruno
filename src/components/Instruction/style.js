import { StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container:{
        flexDirection:"row",
        padding: 10,
        backgroundColor: "#e6e6e6",
        borderRadius: 10,
        marginTop: 5,
        marginBottom: 5,
    },
    text:{
        lineHeight: 20,
        fontSize: 18,
        color: "#000"
    }
}) 


export {styles}